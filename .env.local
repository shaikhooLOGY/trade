# Local Development Environment Configuration
# This overrides .env for local development
APP_ENV=local

# Local Database Configuration
# Using the local MySQL installation
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=traders_local

# Development Email Settings (local)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=
SMTP_USER=
SMTP_PASS=
MAIL_FROM=noreply@localhost
MAIL_FROM_NAME=Shaikhoology - Local Dev

# Site URL for local development
SITE_URL=http://localhost:8000
BASE_URL=http://localhost:8000